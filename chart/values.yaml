replicaCount: 1

image:
  repository: evoapicloud/evolution-api
  tag: dev
  pullPolicy: Always

service:
  port: 8080

secretName: evolution-api-secrets

resources:
  requests:
    cpu: 100m
    memory: 512Mi
  limits:
    cpu: 500m
    memory: 1Gi

env: []
